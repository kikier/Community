<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kikier.community">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.CoverActivity" />
        <activity android:name=".view.NewsCenterActivity" />
        <activity android:name=".view.DetailActivity" />
        <activity android:name=".view.PropertyActivity" />
        <activity android:name=".view.SaleHouseActivity" />
        <activity android:name=".view.ContactActivity" />
        <activity android:name=".view.ShopActivity" />
        <activity android:name=".view.UsedMarketActivity" />
        <activity android:name=".view.RecreationActivity" />
        <activity android:name=".view.DiscountActivity" />
        <activity android:name=".view.AboutUsActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.WeatherActivity" />
        <activity android:name=".view.ContactDetailActivity" />
        <activity android:name=".view.ShopDetailActivity" />
        <activity android:name=".view.UsedMarketPublishActivity" />
        <activity android:name=".view.RegisterActivity"></activity>
    </application>

</manifest>